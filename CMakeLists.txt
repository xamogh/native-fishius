cmake_minimum_required(VERSION 3.24)
project(FishiusAquarium VERSION 0.1.0 LANGUAGES C CXX)
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
option(AQ_BUILD_APP "Build the SDL3 native game" ON)
option(AQ_BUILD_EDITOR "Build Aquarium Studio on desktop" ON)
option(AQ_BUILD_TESTS "Build SDL-independent domain tests" ON)
option(AQ_SANITIZERS "Enable AddressSanitizer and UndefinedBehaviorSanitizer" OFF)
option(AQ_FETCH_DEPENDENCIES "Download pinned dependencies if missing" ON)
include(FetchContent)
find_package(nlohmann_json 3.11 CONFIG QUIET)
if(NOT TARGET nlohmann_json::nlohmann_json)
 if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/third_party/nlohmann/json.hpp")
  add_library(aq_json INTERFACE)
  target_include_directories(aq_json INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/third_party")
  add_library(nlohmann_json::nlohmann_json ALIAS aq_json)
 elseif(AQ_FETCH_DEPENDENCIES)
  FetchContent_Declare(json URL https://codeload.github.com/nlohmann/json/tar.gz/refs/tags/v3.11.3 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(json)
 else()
  message(FATAL_ERROR "Install nlohmann_json 3.11.3 or enable dependency fetching")
 endif()
endif()
add_library(aquarium_domain src/domain.cpp src/economy.cpp src/decor.cpp)
target_include_directories(aquarium_domain PUBLIC include)
target_link_libraries(aquarium_domain PUBLIC nlohmann_json::nlohmann_json)
add_library(aquarium_storage src/storage.cpp)
target_link_libraries(aquarium_storage PUBLIC aquarium_domain)
function(aq_options target)
 if(MSVC)
  target_compile_options(${target} PRIVATE /W4 /permissive-)
 else()
  target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic -Wshadow)
  if(AQ_SANITIZERS)
   target_compile_options(${target} PUBLIC -fsanitize=address,undefined -fno-omit-frame-pointer)
   target_link_options(${target} PUBLIC -fsanitize=address,undefined)
  endif()
 endif()
endfunction()
aq_options(aquarium_domain)
aq_options(aquarium_storage)
add_library(aquarium_design src/dialog_design.cpp src/dialog_layout.cpp src/ui_project.cpp src/ui_defaults.cpp third_party/clay/clay.c)
target_include_directories(aquarium_design PUBLIC include PRIVATE third_party/clay)
target_link_libraries(aquarium_design PUBLIC nlohmann_json::nlohmann_json)
aq_options(aquarium_design)
if(AQ_BUILD_APP)
 find_package(SDL3 3.2 CONFIG QUIET)
 if(NOT TARGET SDL3::SDL3)
  if(NOT AQ_FETCH_DEPENDENCIES)
   message(FATAL_ERROR "SDL3 3.2.20 is required")
  endif()
  set(SDL_TESTS OFF CACHE BOOL "" FORCE)
  set(SDL_TEST_LIBRARY OFF CACHE BOOL "" FORCE)
  set(SDL_EXAMPLES OFF CACHE BOOL "" FORCE)
  if(ANDROID)
   set(SDL_SHARED ON CACHE BOOL "" FORCE)
   set(SDL_STATIC OFF CACHE BOOL "" FORCE)
  else()
   set(SDL_SHARED OFF CACHE BOOL "" FORCE)
   set(SDL_STATIC ON CACHE BOOL "" FORCE)
  endif()
  FetchContent_Declare(SDL3 URL https://codeload.github.com/libsdl-org/SDL/tar.gz/refs/tags/release-3.2.20 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(SDL3)
 endif()
 find_package(SDL3_image 3.2 CONFIG QUIET)
 if(NOT TARGET SDL3_image::SDL3_image)
  set(SDLIMAGE_SAMPLES OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_TESTS OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_AVIF OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_JXL OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_WEBP OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_TIF OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_VENDORED OFF CACHE BOOL "" FORCE)
  if(IOS)
   set(SDLIMAGE_BACKEND_IMAGEIO OFF CACHE BOOL "Use the portable image loader on iOS" FORCE)
  endif()
  FetchContent_Declare(SDL3_image URL https://codeload.github.com/libsdl-org/SDL_image/tar.gz/refs/tags/release-3.2.4 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(SDL3_image)
 endif()
 find_package(SDL3_ttf 3.2 CONFIG QUIET)
 if(NOT TARGET SDL3_ttf::SDL3_ttf)
  set(SDLTTF_SAMPLES OFF CACHE BOOL "" FORCE)
  set(SDLTTF_TESTS OFF CACHE BOOL "" FORCE)
  set(SDLTTF_VENDORED OFF CACHE BOOL "" FORCE)
  set(SDLTTF_HARFBUZZ OFF CACHE BOOL "" FORCE)
  FetchContent_Declare(SDL3_ttf URL https://codeload.github.com/libsdl-org/SDL_ttf/tar.gz/refs/tags/release-3.2.2 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(SDL3_ttf)
 endif()
 add_library(aquarium_view src/canvas.cpp src/view.cpp src/selection.cpp src/currency_shop.cpp src/message_dialog.cpp src/ui_renderer.cpp src/ui_view.cpp src/hud.cpp src/hud_placement.cpp src/shop_theme.cpp src/level_up.cpp src/tanks.cpp src/skin.cpp src/theme.cpp src/details.cpp src/menu_motion.cpp src/loading.cpp src/decor_view.cpp src/decor_canvas.cpp src/tool_cursor.cpp)
 target_include_directories(aquarium_view PRIVATE third_party/clay)
 target_link_libraries(aquarium_view PUBLIC aquarium_storage aquarium_design SDL3::SDL3 SDL3_image::SDL3_image SDL3_ttf::SDL3_ttf)
 aq_options(aquarium_view)
 if(ANDROID)
  add_library(main SHARED src/main.cpp)
  target_link_libraries(main PRIVATE aquarium_view)
  set(AQ_EXECUTABLE main)
 elseif(IOS)
  add_executable(aquarium MACOSX_BUNDLE src/main.cpp)
  target_link_libraries(aquarium PRIVATE aquarium_view)
  set_target_properties(aquarium PROPERTIES MACOSX_BUNDLE_GUI_IDENTIFIER "com.pixmot.aquarium" MACOSX_BUNDLE_BUNDLE_NAME "Fishius" MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/platform/ios/Info.plist" XCODE_ATTRIBUTE_TARGETED_DEVICE_FAMILY "1,2" XCODE_ATTRIBUTE_PRODUCT_BUNDLE_IDENTIFIER "com.pixmot.aquarium")
  # Copy the whole asset tree after every link. A configure-time glob would
  # miss assets regenerated after the build tree was configured, and would
  # silently drop file types absent from its pattern list.
  add_custom_command(TARGET aquarium POST_BUILD
   COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/assets" "$<TARGET_BUNDLE_DIR:aquarium>/assets")
  set(AQ_EXECUTABLE aquarium)
 else()
  add_executable(aquarium src/main.cpp)
  target_link_libraries(aquarium PRIVATE aquarium_view)
  add_custom_command(TARGET aquarium POST_BUILD COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/assets" "$<TARGET_FILE_DIR:aquarium>/assets")
  install(TARGETS aquarium RUNTIME DESTINATION .)
  install(DIRECTORY assets DESTINATION . PATTERN "workbook.json" EXCLUDE)
  set(AQ_EXECUTABLE aquarium)
 endif()
 # Asset edits must refresh the installed app even when no C++ file changed.
 file(GLOB_RECURSE AQ_ASSET_FILES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/assets/*")
 set_property(TARGET ${AQ_EXECUTABLE} APPEND PROPERTY LINK_DEPENDS "${AQ_ASSET_FILES}")
 aq_options(${AQ_EXECUTABLE})
 if(AQ_BUILD_EDITOR AND NOT ANDROID AND NOT IOS)
  add_library(aquarium_imgui STATIC third_party/imgui/imgui.cpp third_party/imgui/imgui_draw.cpp
   third_party/imgui/imgui_tables.cpp third_party/imgui/imgui_widgets.cpp
   third_party/imgui/backends/imgui_impl_sdl3.cpp third_party/imgui/backends/imgui_impl_sdlrenderer3.cpp)
  target_include_directories(aquarium_imgui PUBLIC third_party/imgui third_party/imgui/backends)
  target_link_libraries(aquarium_imgui PUBLIC SDL3::SDL3)
  add_executable(aquarium_studio src/studio.cpp src/studio_panels.cpp src/studio_canvas.cpp)
  target_link_libraries(aquarium_studio PRIVATE aquarium_view aquarium_imgui)
  target_compile_definitions(aquarium_studio PRIVATE AQ_STUDIO_DEFAULT_ASSETS="${CMAKE_CURRENT_SOURCE_DIR}/assets")
  if(APPLE)
   set_target_properties(aquarium_studio PROPERTIES MACOSX_BUNDLE TRUE
    MACOSX_BUNDLE_GUI_IDENTIFIER "com.pixmot.aquarium.studio"
    MACOSX_BUNDLE_BUNDLE_NAME "Aquarium Studio" OUTPUT_NAME "Aquarium Studio")
   add_custom_command(TARGET aquarium_studio POST_BUILD
    COMMAND ${CMAKE_COMMAND} -E create_symlink "$<TARGET_FILE:aquarium_studio>" "${CMAKE_CURRENT_BINARY_DIR}/aquarium_studio")
  endif()
  aq_options(aquarium_studio)
 endif()
endif()
if(AQ_BUILD_TESTS AND NOT ANDROID AND NOT IOS)
 enable_testing()
 add_executable(aquarium_dialog_design_tests tests/dialog_design_tests.cpp)
 target_link_libraries(aquarium_dialog_design_tests PRIVATE aquarium_design)
 aq_options(aquarium_dialog_design_tests)
 add_test(NAME dialog_design_document COMMAND aquarium_dialog_design_tests)
 add_executable(aquarium_v4_tests tests/v4_tests.cpp)
 target_link_libraries(aquarium_v4_tests PRIVATE aquarium_storage)
 aq_options(aquarium_v4_tests)
 add_test(NAME v4_lifecycle_and_transactions COMMAND aquarium_v4_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_tests tests/domain_tests.cpp)
 target_link_libraries(aquarium_tests PRIVATE aquarium_storage)
 aq_options(aquarium_tests)
 add_test(NAME domain_contract COMMAND aquarium_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_level_reward_tests tests/level_reward_tests.cpp)
 target_link_libraries(aquarium_level_reward_tests PRIVATE aquarium_storage)
 aq_options(aquarium_level_reward_tests)
 add_test(NAME level_rewards COMMAND aquarium_level_reward_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_decor_tests tests/decor_tests.cpp)
 target_link_libraries(aquarium_decor_tests PRIVATE aquarium_storage)
 aq_options(aquarium_decor_tests)
 add_test(NAME decor_contract COMMAND aquarium_decor_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_storage_feedback_tests tests/storage_feedback_tests.cpp)
 target_link_libraries(aquarium_storage_feedback_tests PRIVATE aquarium_storage)
 aq_options(aquarium_storage_feedback_tests)
 add_test(NAME storage_feedback COMMAND aquarium_storage_feedback_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 if(AQ_BUILD_APP)
  if(TARGET aquarium_studio)
   add_test(NAME studio_editor_gestures COMMAND aquarium_studio --software --smoke --frames 25 --report "${CMAKE_CURRENT_BINARY_DIR}/studio-smoke.json")
   set_tests_properties(studio_editor_gestures PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  endif()
  add_executable(aquarium_ui_project_tests tests/ui_project_tests.cpp)
  target_link_libraries(aquarium_ui_project_tests PRIVATE aquarium_design)
  aq_options(aquarium_ui_project_tests)
  add_test(NAME studio_project_recovery COMMAND aquarium_ui_project_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_ui_runtime_tests tests/ui_runtime_tests.cpp)
  target_link_libraries(aquarium_ui_runtime_tests PRIVATE aquarium_view)
  aq_options(aquarium_ui_runtime_tests)
  add_test(NAME studio_v2_runtime COMMAND aquarium_ui_runtime_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_studio_preview_tests tests/studio_preview_tests.cpp)
  target_link_libraries(aquarium_studio_preview_tests PRIVATE aquarium_view)
  aq_options(aquarium_studio_preview_tests)
  add_test(NAME studio_game_preview_parity COMMAND aquarium_studio_preview_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(studio_game_preview_parity PROPERTIES LABELS "native;ui")
  add_executable(aquarium_egg_placement_tests tests/egg_placement_tests.cpp)
  target_link_libraries(aquarium_egg_placement_tests PRIVATE aquarium_view)
  aq_options(aquarium_egg_placement_tests)
  add_test(NAME egg_placement_controls COMMAND aquarium_egg_placement_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(egg_placement_controls PROPERTIES LABELS "native;ui")
  add_executable(aquarium_level_up_tests tests/level_up_tests.cpp)
  target_link_libraries(aquarium_level_up_tests PRIVATE aquarium_view)
  aq_options(aquarium_level_up_tests)
  add_test(NAME level_up_controls COMMAND aquarium_level_up_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_settings_catalog_tests tests/settings_catalog_tests.cpp)
  target_link_libraries(aquarium_settings_catalog_tests PRIVATE aquarium_view)
  aq_options(aquarium_settings_catalog_tests)
  add_test(NAME settings_and_seasonal_catalog COMMAND aquarium_settings_catalog_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_purchase_ui_tests tests/purchase_ui_tests.cpp)
  target_link_libraries(aquarium_purchase_ui_tests PRIVATE aquarium_view)
  aq_options(aquarium_purchase_ui_tests)
  add_test(NAME purchase_and_save_controls COMMAND aquarium_purchase_ui_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_tanks_tests tests/tanks_tests.cpp)
  target_link_libraries(aquarium_tanks_tests PRIVATE aquarium_view)
  aq_options(aquarium_tanks_tests)
  add_test(NAME tank_menu_controls COMMAND aquarium_tanks_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(tank_menu_controls PROPERTIES LABELS "native;ui")
  add_executable(aquarium_view_tests tests/view_tests.cpp)
  target_link_libraries(aquarium_view_tests PRIVATE aquarium_view)
  aq_options(aquarium_view_tests)
  add_test(NAME mobile_ui_controls COMMAND aquarium_view_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_selection_tests tests/selection_tests.cpp)
  target_link_libraries(aquarium_selection_tests PRIVATE aquarium_view)
  aq_options(aquarium_selection_tests)
  add_test(NAME direct_selection COMMAND aquarium_selection_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_currency_shop_tests tests/currency_shop_tests.cpp)
  target_link_libraries(aquarium_currency_shop_tests PRIVATE aquarium_view)
  aq_options(aquarium_currency_shop_tests)
  add_test(NAME currency_shop_controls COMMAND aquarium_currency_shop_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(direct_selection PROPERTIES LABELS "native;ui")
  add_executable(aquarium_decor_view_tests tests/decor_view_tests.cpp)
  target_link_libraries(aquarium_decor_view_tests PRIVATE aquarium_view)
  aq_options(aquarium_decor_view_tests)
  add_test(NAME decor_ui_controls COMMAND aquarium_decor_view_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(decor_ui_controls PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}")
  set_tests_properties(mobile_ui_controls PROPERTIES LABELS "native;ui")
  add_executable(aquarium_details_tests tests/details_tests.cpp)
  target_link_libraries(aquarium_details_tests PRIVATE aquarium_view)
  aq_options(aquarium_details_tests)
  add_test(NAME fish_details_controls COMMAND aquarium_details_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(fish_details_controls PROPERTIES LABELS "native;ui")
  add_executable(aquarium_rendering_tests tests/rendering_tests.cpp)
  target_link_libraries(aquarium_rendering_tests PRIVATE aquarium_view)
  aq_options(aquarium_rendering_tests)
  add_test(NAME rendering_quality COMMAND aquarium_rendering_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(rendering_quality PROPERTIES LABELS "native;ui")
  add_executable(aquarium_tool_cursor_tests tests/tool_cursor_tests.cpp)
  target_link_libraries(aquarium_tool_cursor_tests PRIVATE aquarium_view)
  aq_options(aquarium_tool_cursor_tests)
  add_test(NAME tool_cursor_controls COMMAND aquarium_tool_cursor_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(tool_cursor_controls PROPERTIES WORKING_DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}" LABELS "native;ui")
  add_executable(aquarium_menu_motion_tests tests/menu_motion_tests.cpp)
  target_link_libraries(aquarium_menu_motion_tests PRIVATE aquarium_view)
  aq_options(aquarium_menu_motion_tests)
  add_test(NAME menu_motion COMMAND aquarium_menu_motion_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(menu_motion PROPERTIES LABELS "native;ui")
  add_executable(aquarium_startup_tests tests/startup_tests.cpp)
  target_link_libraries(aquarium_startup_tests PRIVATE aquarium_view)
  aq_options(aquarium_startup_tests)
  add_test(NAME startup_and_shop_swipes COMMAND aquarium_startup_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(startup_and_shop_swipes PROPERTIES LABELS "native;ui")
  add_executable(aquarium_hud_tests tests/hud_tests.cpp)
  target_link_libraries(aquarium_hud_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_tests)
  add_test(NAME clay_hud_layout COMMAND aquarium_hud_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
 endif()
endif()
set(CPACK_PACKAGE_NAME "Fishius")
set(CPACK_PACKAGE_VERSION "0.1.0")
set(CPACK_GENERATOR "TGZ")
include(CPack)
