cmake_minimum_required(VERSION 3.24)
project(FishiusAquarium VERSION 0.1.0 LANGUAGES C CXX)
if(IOS)
 enable_language(OBJCXX)
endif()
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
option(AQ_BUILD_APP "Build the SDL3 native game" ON)
option(AQ_BUILD_TESTS "Build SDL-independent domain tests" ON)
option(AQ_SANITIZERS "Enable AddressSanitizer and UndefinedBehaviorSanitizer" OFF)
option(AQ_FETCH_DEPENDENCIES "Download pinned dependencies if missing" ON)
include(FetchContent)
find_package(nlohmann_json 3.11 CONFIG QUIET)
if(NOT TARGET nlohmann_json::nlohmann_json)
 if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/third_party/nlohmann/json.hpp")
  add_library(aq_json INTERFACE)
  target_include_directories(aq_json INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/third_party")
  add_library(nlohmann_json::nlohmann_json ALIAS aq_json)
 elseif(AQ_FETCH_DEPENDENCIES)
  FetchContent_Declare(json URL https://codeload.github.com/nlohmann/json/tar.gz/refs/tags/v3.11.3 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(json)
 else()
  message(FATAL_ERROR "Install nlohmann_json 3.11.3 or enable dependency fetching")
 endif()
endif()
add_library(aquarium_domain src/domain.cpp src/economy.cpp src/decor.cpp src/environment.cpp)
target_include_directories(aquarium_domain PUBLIC include)
target_link_libraries(aquarium_domain PUBLIC nlohmann_json::nlohmann_json)
add_library(aquarium_storage src/storage.cpp)
target_link_libraries(aquarium_storage PUBLIC aquarium_domain)
function(aq_options target)
 if(MSVC)
  target_compile_options(${target} PRIVATE /W4 /permissive-)
 else()
  target_compile_options(${target} PRIVATE -Wall -Wextra -Wpedantic -Wshadow)
  if(AQ_SANITIZERS)
   target_compile_options(${target} PUBLIC -fsanitize=address,undefined -fno-omit-frame-pointer)
   target_link_options(${target} PUBLIC -fsanitize=address,undefined)
  endif()
 endif()
endfunction()
aq_options(aquarium_domain)
aq_options(aquarium_storage)
if(AQ_BUILD_APP)
 set(AQ_SDL_MIN_VERSION 3.2)
 set(AQ_SDL_FETCH_VERSION 3.2.20)
 if(IOS)
  # iOS 27 requires the scene lifecycle provided by SDL 3.4.
  set(AQ_SDL_MIN_VERSION 3.4.12)
  set(AQ_SDL_FETCH_VERSION 3.4.12)
 endif()
 find_package(SDL3 ${AQ_SDL_MIN_VERSION} CONFIG QUIET)
 if(NOT TARGET SDL3::SDL3)
  if(NOT AQ_FETCH_DEPENDENCIES)
   message(FATAL_ERROR "SDL3 ${AQ_SDL_MIN_VERSION} or newer is required")
  endif()
  set(SDL_TESTS OFF CACHE BOOL "" FORCE)
  set(SDL_TEST_LIBRARY OFF CACHE BOOL "" FORCE)
  set(SDL_EXAMPLES OFF CACHE BOOL "" FORCE)
  if(ANDROID)
   set(SDL_SHARED ON CACHE BOOL "" FORCE)
   set(SDL_STATIC OFF CACHE BOOL "" FORCE)
  else()
   set(SDL_SHARED OFF CACHE BOOL "" FORCE)
   set(SDL_STATIC ON CACHE BOOL "" FORCE)
  endif()
  FetchContent_Declare(SDL3 URL https://codeload.github.com/libsdl-org/SDL/tar.gz/refs/tags/release-${AQ_SDL_FETCH_VERSION} DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(SDL3)
  if(IOS)
   get_directory_property(AQ_SDL_SOURCE_VERSION DIRECTORY "${sdl3_SOURCE_DIR}" DEFINITION SDL3_VERSION)
   if(AQ_SDL_SOURCE_VERSION VERSION_LESS AQ_SDL_MIN_VERSION)
    message(FATAL_ERROR "iOS requires SDL ${AQ_SDL_MIN_VERSION} or newer for the scene lifecycle. Update FETCHCONTENT_SOURCE_DIR_SDL3 or clear it to use the pinned release.")
   endif()
  endif()
 endif()
 find_package(SDL3_image 3.2 CONFIG QUIET)
 if(NOT TARGET SDL3_image::SDL3_image)
  set(SDLIMAGE_SAMPLES OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_TESTS OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_AVIF OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_JXL OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_WEBP OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_TIF OFF CACHE BOOL "" FORCE)
  set(SDLIMAGE_VENDORED OFF CACHE BOOL "" FORCE)
  if(IOS)
   set(SDLIMAGE_BACKEND_IMAGEIO OFF CACHE BOOL "Use the portable image loader on iOS" FORCE)
  endif()
  FetchContent_Declare(SDL3_image URL https://codeload.github.com/libsdl-org/SDL_image/tar.gz/refs/tags/release-3.2.4 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(SDL3_image)
 endif()
 find_package(SDL3_ttf 3.2 CONFIG QUIET)
 if(NOT TARGET SDL3_ttf::SDL3_ttf)
  set(SDLTTF_SAMPLES OFF CACHE BOOL "" FORCE)
  set(SDLTTF_TESTS OFF CACHE BOOL "" FORCE)
  set(SDLTTF_VENDORED OFF CACHE BOOL "" FORCE)
  set(SDLTTF_HARFBUZZ OFF CACHE BOOL "" FORCE)
  FetchContent_Declare(SDL3_ttf URL https://codeload.github.com/libsdl-org/SDL_ttf/tar.gz/refs/tags/release-3.2.2 DOWNLOAD_EXTRACT_TIMESTAMP TRUE)
  FetchContent_MakeAvailable(SDL3_ttf)
 endif()
 add_library(aquarium_clay STATIC third_party/clay/clay.c)
 target_include_directories(aquarium_clay PUBLIC third_party/clay)
 add_library(aquarium_view
  src/canvas.cpp src/environment_canvas.cpp src/decor_canvas.cpp src/loading.cpp
  src/hud.cpp src/hud_tank_switcher.cpp src/hud_placement.cpp src/hud_care.cpp src/hud_sell_layout.cpp src/hud_rewards.cpp src/hud_funds.cpp src/hud_toast.cpp
  src/shop_theme.cpp src/fish_details.cpp src/tool_cursor.cpp src/hud_level_up.cpp src/hud_decor_placement.cpp src/hud_layout_editor.cpp
  src/hud_settings.cpp src/game_audio.cpp src/dialog_motion.cpp)
 target_compile_definitions(aquarium_view PUBLIC AQ_VERSION="${PROJECT_VERSION}")
 if(IOS)
  set_property(TARGET aquarium_view PROPERTY OBJCXX_STANDARD 20)
  target_sources(aquarium_view PRIVATE platform/ios/feedback.mm)
  target_link_libraries(aquarium_view PUBLIC "-framework UIKit")
 else()
  target_sources(aquarium_view PRIVATE src/platform_feedback.cpp)
 endif()
 target_link_libraries(aquarium_view PUBLIC aquarium_storage aquarium_clay SDL3::SDL3 SDL3_image::SDL3_image SDL3_ttf::SDL3_ttf)
 aq_options(aquarium_view)
 if(ANDROID)
  add_library(main SHARED src/main.cpp)
  target_link_libraries(main PRIVATE aquarium_view)
  set(AQ_EXECUTABLE main)
 elseif(IOS)
  add_executable(aquarium MACOSX_BUNDLE src/main.cpp)
  target_link_libraries(aquarium PRIVATE aquarium_view)
  set_target_properties(aquarium PROPERTIES MACOSX_BUNDLE_GUI_IDENTIFIER "com.pixmot.aquarium" MACOSX_BUNDLE_BUNDLE_NAME "Fishius" MACOSX_BUNDLE_INFO_PLIST "${CMAKE_CURRENT_SOURCE_DIR}/platform/ios/Info.plist" XCODE_ATTRIBUTE_TARGETED_DEVICE_FAMILY "1,2" XCODE_ATTRIBUTE_PRODUCT_BUNDLE_IDENTIFIER "com.pixmot.aquarium")
  # Replace the bundle assets so removed artwork cannot survive a rebuild.
  add_custom_command(TARGET aquarium POST_BUILD
   COMMAND ${CMAKE_COMMAND} -E rm -rf "$<TARGET_BUNDLE_DIR:aquarium>/assets"
   COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/assets" "$<TARGET_BUNDLE_DIR:aquarium>/assets")
  set(AQ_EXECUTABLE aquarium)
 else()
  add_executable(aquarium src/main.cpp)
  target_link_libraries(aquarium PRIVATE aquarium_view)
  add_custom_command(TARGET aquarium POST_BUILD
   COMMAND ${CMAKE_COMMAND} -E rm -rf "$<TARGET_FILE_DIR:aquarium>/assets"
   COMMAND ${CMAKE_COMMAND} -E copy_directory "${CMAKE_CURRENT_SOURCE_DIR}/assets" "$<TARGET_FILE_DIR:aquarium>/assets")
  install(TARGETS aquarium RUNTIME DESTINATION .)
  install(DIRECTORY assets DESTINATION . PATTERN "workbook.json" EXCLUDE)
  set(AQ_EXECUTABLE aquarium)
 endif()
 # Asset edits must refresh the installed app even when no C++ file changed.
 file(GLOB_RECURSE AQ_ASSET_FILES CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/assets/*")
 # A removed file also changes the manifest, forcing a fresh asset copy.
 file(GENERATE OUTPUT "${CMAKE_CURRENT_BINARY_DIR}/asset-inputs.txt" CONTENT "${AQ_ASSET_FILES}\n")
 set_property(TARGET ${AQ_EXECUTABLE} APPEND PROPERTY LINK_DEPENDS "${AQ_ASSET_FILES};${CMAKE_CURRENT_BINARY_DIR}/asset-inputs.txt")
 aq_options(${AQ_EXECUTABLE})

endif()
if(AQ_BUILD_TESTS AND NOT ANDROID AND NOT IOS)
 enable_testing()
 add_executable(aquarium_environment_tests tests/environment_tests.cpp)
 target_link_libraries(aquarium_environment_tests PRIVATE aquarium_storage)
 aq_options(aquarium_environment_tests)
 add_test(NAME environment_contract COMMAND aquarium_environment_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_v4_tests tests/v4_tests.cpp)
 target_link_libraries(aquarium_v4_tests PRIVATE aquarium_storage)
 aq_options(aquarium_v4_tests)
 add_test(NAME v4_lifecycle_and_transactions COMMAND aquarium_v4_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_tests tests/domain_tests.cpp)
 target_link_libraries(aquarium_tests PRIVATE aquarium_storage)
 aq_options(aquarium_tests)
 add_test(NAME domain_contract COMMAND aquarium_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_level_reward_tests tests/level_reward_tests.cpp)
 target_link_libraries(aquarium_level_reward_tests PRIVATE aquarium_storage)
 aq_options(aquarium_level_reward_tests)
 add_test(NAME level_rewards COMMAND aquarium_level_reward_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_economy_balance_tests tests/economy_balance_tests.cpp)
 target_link_libraries(aquarium_economy_balance_tests PRIVATE aquarium_storage)
 aq_options(aquarium_economy_balance_tests)
 add_test(NAME economy_balance COMMAND aquarium_economy_balance_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_decor_tests tests/decor_tests.cpp)
 target_link_libraries(aquarium_decor_tests PRIVATE aquarium_storage)
 aq_options(aquarium_decor_tests)
 add_test(NAME decor_contract COMMAND aquarium_decor_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 add_executable(aquarium_storage_feedback_tests tests/storage_feedback_tests.cpp)
 target_link_libraries(aquarium_storage_feedback_tests PRIVATE aquarium_storage)
 aq_options(aquarium_storage_feedback_tests)
 add_test(NAME storage_feedback COMMAND aquarium_storage_feedback_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets/content.json")
 if(AQ_BUILD_APP)
  add_executable(aquarium_dialog_motion_tests tests/dialog_motion_tests.cpp)
  target_link_libraries(aquarium_dialog_motion_tests PRIVATE aquarium_view)
  aq_options(aquarium_dialog_motion_tests)
  add_test(NAME dialog_motion COMMAND aquarium_dialog_motion_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(dialog_motion PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_scroll_tests tests/scroll_tests.cpp)
  target_link_libraries(aquarium_scroll_tests PRIVATE aquarium_view)
  aq_options(aquarium_scroll_tests)
  add_test(NAME menu_scrolling COMMAND aquarium_scroll_tests)
  add_executable(aquarium_hud_settings_tests tests/hud_settings_tests.cpp)
  target_link_libraries(aquarium_hud_settings_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_settings_tests)
  add_test(NAME settings_controls COMMAND aquarium_hud_settings_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets" "${CMAKE_CURRENT_BINARY_DIR}/settings-captures")
  set_tests_properties(settings_controls PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_environment_render_tests tests/environment_render_tests.cpp)
  target_link_libraries(aquarium_environment_render_tests PRIVATE aquarium_view)
  aq_options(aquarium_environment_render_tests)
  add_test(NAME environment_rendering COMMAND aquarium_environment_render_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets" "${CMAKE_CURRENT_BINARY_DIR}/environment-captures")
  set_tests_properties(environment_rendering PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_rendering_tests tests/rendering_tests.cpp)
  target_link_libraries(aquarium_rendering_tests PRIVATE aquarium_view)
  aq_options(aquarium_rendering_tests)
  add_test(NAME rendering_quality COMMAND aquarium_rendering_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(rendering_quality PROPERTIES LABELS "native;ui")
  add_executable(aquarium_tool_cursor_tests tests/tool_cursor_tests.cpp)
  target_link_libraries(aquarium_tool_cursor_tests PRIVATE aquarium_view)
  aq_options(aquarium_tool_cursor_tests)
  add_test(NAME tool_cursor_animation COMMAND aquarium_tool_cursor_tests)
  add_executable(aquarium_hud_tests tests/hud_tests.cpp)
  target_link_libraries(aquarium_hud_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_tests)
  add_test(NAME clay_hud_layout COMMAND aquarium_hud_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_hud_decor_placement_tests tests/hud_decor_placement_tests.cpp)
  target_link_libraries(aquarium_hud_decor_placement_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_decor_placement_tests)
  add_test(NAME clay_decor_placement COMMAND aquarium_hud_decor_placement_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets" "${CMAKE_CURRENT_BINARY_DIR}/decor-placement-captures")
  set_tests_properties(clay_decor_placement PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_hud_layout_editor_tests tests/hud_layout_editor_tests.cpp)
  target_link_libraries(aquarium_hud_layout_editor_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_layout_editor_tests)
  add_test(NAME clay_layout_editor COMMAND aquarium_hud_layout_editor_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets" "${CMAKE_CURRENT_BINARY_DIR}/layout-editor-captures")
  set_tests_properties(clay_layout_editor PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_tank_switcher_tests tests/tank_switcher_tests.cpp)
  target_link_libraries(aquarium_tank_switcher_tests PRIVATE aquarium_view)
  aq_options(aquarium_tank_switcher_tests)
  add_test(NAME clay_tank_switcher_and_shop COMMAND aquarium_tank_switcher_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  add_executable(aquarium_menu_loading_tests tests/menu_loading_tests.cpp)
  target_link_libraries(aquarium_menu_loading_tests PRIVATE aquarium_view)
  aq_options(aquarium_menu_loading_tests)
  add_test(NAME menu_loading COMMAND aquarium_menu_loading_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(menu_loading PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_hud_funds_tests tests/hud_funds_tests.cpp)
  target_link_libraries(aquarium_hud_funds_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_funds_tests)
  add_test(NAME clay_currency_shortfalls COMMAND aquarium_hud_funds_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(clay_currency_shortfalls PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_hud_level_up_tests tests/hud_level_up_tests.cpp)
  target_link_libraries(aquarium_hud_level_up_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_level_up_tests)
  add_test(NAME clay_level_up_dialog COMMAND aquarium_hud_level_up_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(clay_level_up_dialog PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_hud_care_tests tests/hud_care_tests.cpp)
  target_link_libraries(aquarium_hud_care_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_care_tests)
  add_test(NAME clay_feeding_and_rehoming COMMAND aquarium_hud_care_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(clay_feeding_and_rehoming PROPERTIES LABELS "native;ui")
  add_executable(aquarium_hud_sell_layout_tests tests/hud_sell_layout_tests.cpp)
  target_link_libraries(aquarium_hud_sell_layout_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_sell_layout_tests)
  add_test(NAME clay_sell_spacing COMMAND aquarium_hud_sell_layout_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets" "${CMAKE_CURRENT_BINARY_DIR}/sell-spacing-captures")
  set_tests_properties(clay_sell_spacing PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_executable(aquarium_hud_rewards_tests tests/hud_rewards_tests.cpp)
  target_link_libraries(aquarium_hud_rewards_tests PRIVATE aquarium_view)
  aq_options(aquarium_hud_rewards_tests)
  add_test(NAME clay_reward_collection COMMAND aquarium_hud_rewards_tests "${CMAKE_CURRENT_SOURCE_DIR}/assets")
  set_tests_properties(clay_reward_collection PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
  add_test(NAME current_interface_startup COMMAND ${CMAKE_COMMAND}
   "-DGAME=$<TARGET_FILE:aquarium>" "-DASSETS=${CMAKE_CURRENT_SOURCE_DIR}/assets"
   "-DOUTPUT=${CMAKE_CURRENT_BINARY_DIR}/startup-check" -P "${CMAKE_CURRENT_SOURCE_DIR}/tests/startup_smoke.cmake")
  set_tests_properties(current_interface_startup PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy;SDL_AUDIODRIVER=dummy" LABELS "ui")
 endif()
endif()
set(CPACK_PACKAGE_NAME "Fishius")
set(CPACK_PACKAGE_VERSION "0.1.0")
set(CPACK_GENERATOR "TGZ")
include(CPack)
